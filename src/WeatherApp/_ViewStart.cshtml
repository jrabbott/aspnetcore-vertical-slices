@* Must stay at the web project root (not Views/ only).
   Feature views under Features/ only inherit _ViewStart from ancestors toward the content root.
   See docs/explanation/razor-view-discovery.md. *@
@{
    Layout = "_Layout";
}
